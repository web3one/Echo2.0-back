# ====================================================================
# HD 钱包密钥配置 — 模板文件（committed）
#
# 使用方法：
#   1. 复制本文件为 application-secret.yml（已 gitignore，不会进 git）
#   2. 把 mnemonic 换成你自己离线生成的真实助记词（12 或 24 词）
#   3. 启动时用 secret profile：
#         java -jar echo2-admin.jar --spring.profiles.active=dev,secret
#
# 也可以不用本文件，直接设置环境变量：
#   export WALLET_MNEMONIC="abandon abandon abandon ... about"
#
# ⚠️ 安全警告：
#   · 助记词是所有派生地址的私钥之源，泄露 = 全部用户充值地址被盗
#   · 严禁提交真实助记词到 git
#   · 建议生产环境用 KMS / Vault，开发环境用本地文件
#   · 这里给的助记词只是 BIP39 标准的测试值，**不要在生产用**
# ====================================================================

wallet:
  # 12 词或 24 词，空格分隔
  mnemonic: "abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

  # 可选 passphrase（BIP39 二次密码），通常留空
  passphrase: ""
